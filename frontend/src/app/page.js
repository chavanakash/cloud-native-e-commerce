import HomePage from '../components/HomePage';

export const metadata = {
  title: 'Home | Countryside.com',
  description: 'Discover beautiful handmade products crafted with care.',
};

export default function Page() {
  return <HomePage />;
}
